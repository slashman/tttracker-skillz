{
  "name": "tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Headless time tracker for parallel work, driven by Claude. Captures overlapping work as structured JSON and resolves the overlap into per-task cost.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node ./bin/install-check.mjs"
  }
}
